# Build artifacts
*.o
*.dSYM/
test_runner
test_runner_asan
xor_example
benchmark
gradient_check
market_gen_bin
dqn_trader_bin

# WASM build output (regenerated by `make wasm`). Committed so Vercel can serve
# `web/` as a pre-built static deploy without installing Emscripten on its end.

# Local-only smoke test for in-browser WASM
web/smoke.html
web/diag.html

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Debug
core
vgcore.*
